# ilvl 200 档复跑（2026-09-24）：由 mod-raidtest-scenario-heroic-an-krikthir-n5.conf 复制，仅 RosterFile 换为 heroic5gear-n5talents-v1（天赋/雕纹/补给与 normal5 相同）。
# 艾卓-尼鲁布（Azjol-Nerub，map 601）英雄：门卫克里克希尔(28684) + 三守望者组，完整遭遇战。
# 2026-09-12 新建（用户指定的第三个副本）。装备档 normal5-v1（ilvl 上限 187），难度英雄，BotCheats 为空。
#
# 场景范围 = 真实遭遇战，未改小：
#   守望者三组共 9 只（三个守望者各带 2 只随从）是 boss 脚本的一部分，不是普通路怪——
#   instance_azjol_nerub.cpp 把它们的 evade 和死亡都接到克里克希尔身上
#   （OnCreatureEvade -> krikthir->EnterEvadeMode；OnUnitDeath -> ACTION_MINION_DIED 触发补招），
#   boss_krikthir_the_gatewatcher.cpp 又在第一只随从进战斗后 60s/120s 各"派"一批，
#   英雄 200 秒时 SetInCombatWithZone 亲自下场。所以这 9 只必须清，且清怪有 200 秒硬时限。
#   走廊更上方的两组（127229/127235/127338 中段、127230/127339 巡逻）不在 boss 脚本里，
#   属于普通路怪，与魔枢/UK 各场景一样不纳入本场景。
#
# 准备点 (510.5, 692.5, 775.60)：
#   - vmap 地面 775.60（raidtest los 实测，大厅 y646–710 是平的，z≈775.2–777.3）
#   - 距 boss 49.9 码 > 22 码（英雄 82 级 boss 仇恨半径 = detection 20 − (80−82)）
#   - 距 9 只守望者组 23.0–49.7 码，全部 > 21 码（守望者 81 级 -> 半径 21）——传送进场不会立刻拉怪
#   - 对 9 只全部 los=true（实测，逐只探过）
#   - 拉得动：最近的 12765 是 23.0 码，靠 AttemptRunner::ApproachPrerequisiteTarget 分批接近
#
# ⚠️ 这个点是第二版。第一版 (528, 690) 只核了「距 boss 和距 9 只守望者」，**漏核了场景外的路怪**：
#   中段走廊那组 127235 (536.1,700.1) 只有 12.9 码、127338 (543.7,694.3) 16.3 码、
#   127229 (541.3,701.8) 17.8 码，全在 20–21 码仇恨半径内。run433 冒烟 1.1 秒就被
#   Skirmisher(28734) 和 Warrior(28732) 打上，6 秒内死 1 人、boss 被拖进战斗而作废。
#   **选准备点必须对「场景内前置怪」和「场景外路怪」两类分别核距离。**
#   现点距最近的场外路怪 26.2 码。
# 开怪点 (529.6, 656, 776.8)：boss 正北 9.8 码，地面 776.79，对 boss los=true。
#
# TimeoutSeconds = 420：首轮基线故意给足，避免把"清 9 只 + 打 boss"的正常耗时误记成超时。
# PrerequisiteTimeoutSeconds = 200：与 boss 自己的 200 秒（英雄）SetInCombatWithZone 对齐。
[Scenario]
MapId = 601
BossEntry = 28684
PartySize = 5
DungeonDifficulty = heroic
Strategy = wotlk-an
RosterFile = "mod-raidtest-roster-heroic5gear-n5talents-v1.conf"
GearProfile = none
EngageX = 529.6
EngageY = 656.0
EngageZ = 776.8
EngageO = 4.71
TimeoutSeconds = 420
EngageTrigger = pull
# 守望者组 9 只（guid）：纳吉尔 12760 / 加什拉 12759 / 斯尔希克 12758 + 各自随从
# 顺序自西向东，让队伍从准备点朝 boss 方向推进，背离中段走廊那组路怪。
PrerequisiteSpawns = 12765,12766,12760,12764,12759,12763,12758,12762,12761
PreparationX = 510.5
PreparationY = 692.5
PreparationZ = 775.60
PreparationO = 4.71
PrerequisiteTimeoutSeconds = 200

# 清怪控制链门禁（docs/testing/TRASH-CC-PULL-DESIGN.md）。开这个键同时打开两件事：
#   1. 拉怪被拒时只接近到 kCcApproachDistance = 24 码（仇恨半径之外），不再走到目标脚下。
#      基线 run436 五场都是纳吉尔组 + 加什拉组共 6 只在 3–5 秒内一起进战斗
#      （两个守望者相距仅 15.0 码，走到其中一只的近战距离必然进另一组的 21 码仇恨半径），
#      坦克改为在 24 码外用制裁之手远程开怪，只把目标那一组拉过来。
#   2. 等控制落地再对骷髅下达开怪。
# 本副本小怪全是亡灵（type 6），变形/妖术/闷棍都落不下去，唯一有效的是牧师束缚亡灵（三角图标）。
PrerequisiteCcWaitSeconds = 25
