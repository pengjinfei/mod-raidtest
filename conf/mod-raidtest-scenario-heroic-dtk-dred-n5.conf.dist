# 达克萨隆要塞（map 600）英雄：第三个 boss 恐怖之王德雷德 King Dred(27483)。
# 2026-09-16 新建。装备档 normal5-v1，难度英雄，BotCheats 为空。
#
# 口径 = **隔离 boss 战（夹具移除七只迅猛龙）**，与本副本另外三个 boss 一致。
# 为什么不是「前置清怪」：见文件末尾「两次翻车」。简短版——德雷德**会巡逻**，
# 清完怪之后的恢复窗口里他自己走过来进战斗，run 556 五场全部
# `prerequisite_invalid: boss entered combat during recovery`。
# 而真人打法本来就是先把七只清光再拉 boss，清光之后 Raptor Call 也没弹药可招；
# 夹具移除与「已清完」在效果上等价，只是结论口径要写成隔离形态。
#
# 原始设计说明（保留）：
#
#   boss_dred.cpp 的 Raptor Call(59416) 会在 100 码内挑一只**活着且未进战斗**的
#   Drakkari Scytheclaw(26628)/Gutripper(26641) 拉进来当增援——房间里的七只迅猛龙
#   就是这个机制的弹药，真人打法也是先清掉。
#
# 七只的位置与到 boss(-544.9,-697) 的距离：
#   127452 (-543.2,-702.5)  5.8   ← 贴身，开怪必然一起来，不列为前置
#   127414 (-527.7,-693.9) 17.5   ← 在 boss 22 码仇恨半径内，拉它就等于拉 boss，不列
#   127455 (-541.6,-674.5) 22.7   ← 临界，不列
#   127413 (-533.9,-718.2) 24.7   ← 前置
#   127415 (-541.5,-662.7) 34.5   ← 前置
#   127454 (-519.1,-722.7) 35.9   ← 前置
#   127453 (-527.8,-659.7) 41.2   ← 前置
#   前置顺序按「离准备点由近到远」排，避免来回穿过房间。
#
# ⚠ 第一版列了四只（127454,127413,127453,127415），run 552 冒烟**在第三只上翻车**：
#   队伍清完东南两只后北上去打 127453(-527.8,-659.7)，路径必然穿过 boss 的 22 码半径，
#   德雷德 61 秒自己进了战斗并走到 (-535.5,-702.7) → `prerequisite_invalid: boss engaged
#   before clearing completed`。**北边那两只在不进 boss 仇恨半径的前提下够不着**，
#   所以只清东南两只，北边两只留着当 Raptor Call 的弹药。这是房间本身的约束，不是简化。
#
# 准备点 (-500,-735,30.3)：房间东南口外。距 boss 58.8 码、对 boss los=true；
#   距最近的迅猛龙 127454 为 22.7 码（英雄迅猛龙仇恨半径约 20 + 体型）。
#   ⚠ 这一片**没有 vmap 地面**（房间地板是地形高度图，不是模型），`raidtest los` 的
#   vmap_floor 读不出来；z=30.3 取自附近 spawn（迅猛龙 28.6–31、Elder Kilias 30.3）。
#   **首轮冒烟必须确认 bot 真的能从这里走出去**（位移探针），卡住就换点。
# 开怪点 (-551,-685,30.3)：德雷德的巡逻是 14 点 waypoint 路径（`waypoint_data` id 1275070，
#   8 个不同坐标，从 (-529.8,-711.5) 经 (-544.9,-697)/(-554.6,-681.4) 到 (-535.8,-664.3) 再折回）。
#   (-551,-685) 是实测唯一对**全部 8 个 waypoint** 都 los=true 的点（最远 33.9 码，
#   超出施法距离的那一刻靠框架的开怪重试等他走近）。次选 (-549,-690)：6/8 视线、最远 28.9 码。
#
# 历史（第一版开怪点）：(-536,-690,30.3)——**德雷德会巡逻**，出生点 (-544.9,-697) 只是他路过的一个位置——
#   run 554 开怪那一刻他已经走到 (-539.2,-668.7)，坦克站在原来的 (-538,-697) 对他 los=false，
#   直接 `pull failed (boss not engaged)`。所以开怪点必须对**整条巡逻线**都有视线：
#   (-536,-690) 对 (-544.9,-697) / (-539.2,-668.7) / (-541,-710) / (-535,-655) 四点实测全 los=true，
#   距离 11–35 码（制裁之手 30 码内可开）。另一个可用点是 (-536,-683)。
#
# TimeoutSeconds = 480 / PrerequisiteTimeoutSeconds = 200。
[Scenario]
MapId = 600
BossEntry = 27483
PartySize = 5
DungeonDifficulty = heroic
Strategy = wotlk-dtk
RosterFile = "mod-raidtest-roster-normal5-v1.conf"
GearProfile = none
EngageX = -551.0
EngageY = -685.0
EngageZ = 30.3
EngageO = 3.14
PreparationX = -500.0
PreparationY = -735.0
PreparationZ = 30.3
PreparationO = 2.36
PrerequisiteSpawns = 127454
PrerequisiteTimeoutSeconds = 120
FixtureDespawnSpawns = 127452,127413,127414,127415,127453,127455,127442,127443,127444
TimeoutSeconds = 480
EngageTrigger = pull

# 127444 = Risen Drakkari Handler，**是 127456/127457 两只迅猛龙的编队队长**
# （`creature_formations`，groupAI=512）。Raptor Call 从 100 码外把迅猛龙拽进战斗，
# 编队把这只精英队长一起带进来——run 559 五场里它每场都在 50–63 秒准时出现，
# 造成 17k–52k。队长移除、两只迅猛龙保留：Raptor Call 的弹药还在，机制不被削。
#
# 127442 / 127443 = Risen Drakkari Handler(26637，精英)：**会巡逻**（127442 是 waypoint、
# 127443 是 5 码游走），从东侧走廊晃进战场。run 557 的致死伤害分解里它反复是最后 10 秒的
# 最大单一来源（6k–28k），也就是说那一轮的 0/5 是「boss + Raptor Call 迅猛龙 + 两只精英路怪」
# 打出来的，基线不干净。它们不属于德雷德的遭遇战（真人打法路上就清了），一并夹具移除。
#
# 为什么还留一只前置（127454）：框架的流程里，**只有走过「前置清怪 → 恢复」这条路，
# 恢复结束时才会把队伍传送到开怪点**（AttemptRunner.cpp 的 Recovery → BossPosition）。
# 没有前置怪的场景，队伍会停在准备点开怪；而这里准备点离 boss 58.8 码，根本开不了。
# 127454 (-519.1,-722.7) 是唯一一只离德雷德巡逻线够远（≥25 码）、清完就地恢复不会
# 把他引过来的。其余六只在 pull 那一刻由夹具移除。
#
# ── 三次翻车（都留着，别重走）────────────────────────────────────────────
# run 552：前置列了四只，清完东南两只后队伍北上打 127453，路径必然穿过 boss 的 22 码
#          半径 → 61 秒 boss 自己进战斗，`prerequisite_invalid: boss engaged before
#          clearing completed`。北边那两只在不进仇恨半径的前提下够不着。
# run 554：前置收到两只、清怪成功（35–40 秒），但开怪被拒——**德雷德会巡逻**，
#          开怪那一刻他在 (-539.2,-668.7)，坦克站在旧开怪点 (-538,-697) 对他 los=false。
#          → 开怪点换成对整条巡逻线都有视线的 (-536,-690)（四点实测全 los=true）。
# run 555：冒烟打起来了 —— **132.7 秒团灭、boss 只掉到 85%**（第一个真实失败点）。
# run 556：五场全部 `boss entered combat during recovery`（清完怪站在他巡逻线旁边）。
#          → 本版改用 FixtureDespawnSpawns。
